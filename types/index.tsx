export interface User {
    username: string;
  }
  
  export interface Student {
    id: string;
    name: string;
    studentId: string;
  }
  
  export interface Certificate {
    id: string;
    name: string;
    issueDate: string;
  }